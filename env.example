# Configuration Prismic
PRISMIC_API_ENDPOINT=https://your-repo.prismic.io/api/v2
PRISMIC_ACCESS_TOKEN=your-access-token-here

# Configuration GSAP (optionnel)
NEXT_PUBLIC_GSAP_LICENSE=your-gsap-license-key

# Configuration OpenAI pour le Chatbot RAG (optionnel mais recommandé)
# Si non définie, le chatbot utilisera un mode de fallback basique
# Pour obtenir une clé API :
# 1. Créez un compte sur https://platform.openai.com
# 2. Allez dans "API keys" → "Create new secret key"
# 3. Copiez la clé (elle commence par sk-proj-...)
# 4. Collez-la ci-dessous
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

